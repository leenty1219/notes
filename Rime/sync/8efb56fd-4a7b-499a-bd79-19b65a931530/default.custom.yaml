# default.custom.yaml
# 只保留小鶴系列方案：小鶴雙拼 + 小鶴音形。
# 在 Control+` 方案選單裡可切換「是否使用音形」：
#   - 小鶴雙拼（純音）
#   - 小鶴音形（音 + 形）
# 列表第一項是預設方案；想改預設，交換兩行順序即可。
#
# 生效方式：選單「鼠鬚管 → 重新部署」，或執行
#   "/Library/Input Methods/Squirrel.app/Contents/MacOS/Squirrel" --reload

patch:
  schema_list:
    - schema: double_pinyin_flypy
    - schema: flypy

  # 每頁候選詞數量：8
  menu:
    page_size: 8
